mod iir_t1;
mod sound_synthesis;
mod transfer_function;
mod vector;
mod rng;
mod image;



pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
